services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: tocayjuga
      POSTGRES_PASSWORD: tocayjuga
      POSTGRES_DB: tocayjuga
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
